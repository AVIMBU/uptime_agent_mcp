{
  "name": "uptime-agent-mcp",
  "version": "0.0.1",
  "description": "MCP Server for uptime-agent.io",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-uptime-agent-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "@types/node": "^22"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.8.2"
  }
}